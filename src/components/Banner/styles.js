import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  bannerContainer: {
    width: '100%',
    height: 200,
    overflow: 'hidden',
    borderRadius: 6,
    marginBottom: 10,
  },
  bannerImage: {
    width: '100%',
    height: '100%',
    borderRadius: 6,
  },
});

export default styles;
